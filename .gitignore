# macOS
.DS_Store

# Node
node_modules/
*.log
.next/
.out/
coverage/

# Dotnet
bin/
obj/
*.user
*.suo

# Docker
**/.env.local

# Misc — secrets, but keep committed templates
.env
.env.*
!.env.template
!**/.env.template

# OMC (oh-my-claudecode) local state
.omc/

# Claude Code local agent config
.claude/

# TypeScript incremental build cache
*.tsbuildinfo

# Local-only seed data (contains real PII; never commit to a public repo)
infra/seed/

# Local build caches / one-off design artifacts
.vite/
/Resume Typst Design.zip
