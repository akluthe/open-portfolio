\
# S1E01 — Monorepo + Baseline Web/API/DB Scaffold

**Why this matters:** Clean foundation shows engineering hygiene and accelerates delivery.

## What we built
- Monorepo structure (`apps`, `packages`, `infra`, `docs`)
- Next.js app (web)
- .NET API skeleton
- Postgres via docker-compose
- Makefile for DX

## Key decisions
- Monorepo (ADR-001)
- Next.js for SSR (ADR-003)
- Postgres JSONB (ADR-004)

## How to run it
```bash
make up   # starts postgres for now
```

## Demo script
1) Show repo tree.
2) Explain intended contracts.
3) Confirm Postgres container is healthy.

## What’s next
- Stand up `packages/shared-types` with a first-pass `ResumeDoc` schema definition.
- Extend `infra/postgres/init.sql` with the `resumes` table and at least one JSONB seed row.
- Add a quick verification step (psql or `make up && make ps`) so viewers know the DB is ready before S1E02.
